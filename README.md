# Text Parsing Project

This project contains downloaded Python wheels for `transformers`, `sentencepiece`, and `spacy` for Python 3.13.

## Setup

1. Clone this repository.
2. Create a virtual environment with Python 3.13.
3. Install the packages from the `wheels` directory:
   ```bash
   pip install --no-index --find-links=wheels -r requirements.txt
   ```
